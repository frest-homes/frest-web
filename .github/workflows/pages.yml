name: Build and deploy site
on:
  push:
    branches: [main]
  workflow_dispatch:
  repository_dispatch:
    types: [rebuild]
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pillow jinja2
      - name: Cache processed images
        uses: actions/cache@v4
        with:
          path: |
            site/img
            assets/manifest.json
          key: img-${{ hashFiles('assets/src/**', 'images.py', 'focal.py') }}
      - run: python3 images.py
      - run: python3 focal.py
      - run: python3 build.py --base "${BASE}" --canonical "${CANONICAL}" --root-lang "${ROOTLANG}" --sister "${SISTER}" ${CNAME:+--cname "$CNAME"}
        env:
          BASE: ${{ vars.SITE_BASE || '/frest-web/' }}
          CANONICAL: ${{ vars.SITE_CANONICAL || 'https://frest-homes.github.io' }}
          ROOTLANG: ${{ vars.SITE_ROOT_LANG || 'lv' }}
          SISTER: ${{ vars.SITE_SISTER || '' }}
          CNAME: ${{ vars.SITE_CNAME }}
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
